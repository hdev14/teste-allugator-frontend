import React from 'react';

import Employees from '../../components/Employees';

const Home = () => {
  return (
    <Employees employees={[]} setEmployees={() => {}} />
  );
};

export default Home;
